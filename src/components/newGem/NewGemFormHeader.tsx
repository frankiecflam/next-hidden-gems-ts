import styles from "./NewGemFormHeader.module.css";

const NewGemFormHeader = () => {
  return (
    <header className={styles.header}>
      <h1 className={styles.heading}>new gem</h1>
    </header>
  );
};

export default NewGemFormHeader;
